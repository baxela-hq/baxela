meta {
  name: SignOut
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/auth/user/account/sign-out
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
