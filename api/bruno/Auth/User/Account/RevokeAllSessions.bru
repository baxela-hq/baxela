meta {
  name: RevokeAllSessions
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v1/auth/user/account/sessions
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
