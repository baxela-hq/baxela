meta {
  name: Sessions
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/auth/user/account/sessions
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
