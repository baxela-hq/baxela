meta {
  name: RevokeSession
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/api/v1/auth/user/account/sessions/:id
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
