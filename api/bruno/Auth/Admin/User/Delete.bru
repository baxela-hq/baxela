meta {
  name: Delete
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/api/v1/auth/admin/users/7
  body: json
  auth: bearer
}

headers {
  Accept: application/json
  Content-Type: application/json
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
