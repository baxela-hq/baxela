meta {
  name: Create
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/auth/admin/roles
  body: json
  auth: bearer
}

headers {
  Accept: application/json
  Content-Type: application/json
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "content-editor",
    "permission_ids": [4, 5]
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
