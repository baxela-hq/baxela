meta {
  name: UpdateStatus
  type: http
  seq: 2
}

patch {
  url: {{baseUrl}}/api/v1/contact/admin/messages/1/status
  body: json
  auth: bearer
}

headers {
  Accept: application/json
  Content-Type: application/json
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "status": "replied"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
