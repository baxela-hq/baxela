meta {
  name: List
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/media/admin/media
  body: none
  auth: bearer
}

params:query {
  ~filter[folder_id]: null
  ~filter[folder_id]: 42
}

headers {
  Accept: application/json
  Content-Type: application/json
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
