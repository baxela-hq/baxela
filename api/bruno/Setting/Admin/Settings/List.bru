meta {
  name: List
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/setting/admin/settings
  body: none
  auth: bearer
}

headers {
  Accept: application/json
  Content-Type: application/json
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
