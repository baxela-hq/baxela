meta {
  name: Delete
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v1/discount/admin/coupons/1
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
