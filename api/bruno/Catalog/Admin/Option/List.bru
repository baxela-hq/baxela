meta {
  name: List
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/v1/catalog/admin/options
  body: none
  auth: bearer
}

params:query {
  ~sort: -position
  ~filter[title]: جیگر
}

headers {
  Accept: application/json
  Content-Type: application/json
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
