meta {
  name: Delete
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/api/v1/catalog/admin/attribute-groups/4
  body: none
  auth: bearer
}

headers {
  Accept: application/json
  Content-Type: application/json
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
